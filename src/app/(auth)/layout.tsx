// Auth pages shell — PLACEHOLDER (sign-in, sign-up, reset password, etc.).
// Real pages land once the auth-strategy milestone (providers, session,
// roles) is decided and Better Auth is actually configured.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
